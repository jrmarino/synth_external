--  This project has been generated by GPRINSTALL 2015 (20150428) (x86_64-portbld-dragonfly4.5)

library project Util is
   type BUILD_KIND is ("static");
   BUILD : BUILD_KIND := external("UTIL_BUILD", "static");

   for Languages use ("Ada");

   case BUILD is
      when "static" =>
         for Source_Dirs use ("../../include/util.static");
         for Library_Dir use "../util.static";
         for Library_Kind use "static";
   end case;

   for Library_Name use "ada_util";

   package Naming is
      for dot_replacement use "-";
      for casing use "lowercase";
      
      case BUILD is
         when "static" =>
            for spec_suffix ("ada") use ".ads";
            for body_suffix ("ada") use ".adb";
            for body ("util.log.loggers.traceback") use "util-log-loggers-traceback-gnat.adb";
      end case;
   end Naming;

   package Linker is
      case BUILD is
         when "static" =>
            null;
      end case;
   end Linker;

   package Install is
      for Active use "False";
   end Install;

   for Externally_Built use "false";
end Util;
